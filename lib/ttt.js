const fs = require('fs')
const { ind } = require('../language')
const { getBuffer } = require('../lib/functions')
const { color, bgcolor } = require('../lib/color')
module.exports = ttt = async ( client , mem , image ) => {
}